# 🎮 Skin Shop API

Простой REST API для игрового магазина скинов. Позволяет пользователям просматривать и покупать скины. Администраторы могут управлять каталогом скинов. Проект реализован на Laravel с использованием Sanctum для авторизации.

---

## 🚀 Основные возможности

- Регистрация и вход пользователей
- Полный CRUD для скинов (с правами администратора)
- Покупка скинов за виртуальные монеты
- Кошелёк с возможностью пополнения
- Система покупок и отслеживания купленных скинов

---
## 🔧 Структура проекта - пример
```text
├── app/
│   ├── Models/
│   │   ├── User.php            → пользователь: имя, email, пароль, is_admin
│   │   ├── Wallet.php          → кошелёк: хранит баланс (coins), связь 1:1 с User
│   │   ├── Skin.php            → скин: название, тип, цена, изображение
│   │   └── Purchase.php        → покупка: связь пользователя и скина
│   └── Policies/
│       └── SkinPolicy.php      → политика: только админ может управлять скинами
│
├── app/Http/Controllers/Api/
│   ├── AuthController.php      → регистрация и вход (авторизация через Sanctum)
│   ├── SkinController.php      → CRUD-операции для скинов
│   ├── WalletController.php    → получение баланса и пополнение кошелька
│   └── PurchaseController.php  → покупка скина и просмотр купленных
│
├── database/
│   ├── migrations/
│   │   ├── create_users_table.php        → таблица пользователей
│   │   ├── create_wallets_table.php      → таблица кошельков
│   │   ├── create_skins_table.php        → таблица скинов
│   │   └── create_purchases_table.php    → таблица покупок
│
├── routes/
│   └── api.php                           → все API-эндпоинты
│
├── .env                                  → файл окружения (настройки БД, ключи)
├── .env.example                          → шаблон .env для копирования
├── composer.json                         → зависимости проекта
└── README.md                             → общее описание проекта
```

## 🔧 Технологии

- **Laravel** 10+
- **PHP** 8.1+
- **MySQL** / SQLite
- **Laravel Sanctum** — API-авторизация
- **REST API** — стандартные HTTP-методы

---

## 📦 Модели

### `User`
- `id`
- `name`
- `email`
- `password`
- `is_admin` (boolean, по умолчанию `false`)

### `Wallet`
- `id`
- `user_id`
- `coins` (по умолчанию `1000`)

### `Skin`
- `id`
- `name`
- `type` (weapon, helmet, cape, armor, boots)
- `price`
- `image_url` (опционально)

### `Purchase`
- `id`
- `user_id`
- `skin_id`
- `created_at`

---



## 🔗 API Endpoints


- POST   /api/register           → регистрация пользователя (создаётся кошелёк с 1000 монет)
- POST   /api/login              → вход в систему, возвращает API-токен
- GET    /api/skins              → список всех скинов (доступно всем)
- GET    /api/skins/{id}         → информация о конкретном скине (доступно всем)
- POST   /api/skins              → создать новый скин (только администратор)
- PUT    /api/skins/{id}         → обновить скин (только администратор)
- DELETE /api/skins/{id}         → удалить скин (только администратор)
- GET    /api/wallet             → получить текущий баланс монет пользователя
- POST   /api/wallet/top-up      → пополнить кошелёк на указанную сумму (авторизованные)
- GET    /api/myskins            → список всех купленных пользователем скинов
- POST   /api/skins/{id}/buy     → купить скин по ID (с проверкой баланса и дубликатов)



### 🔐 Авторизация
POST   /api/register           → регистрация пользователя (создаётся кошелёк)
POST   /api/login              → вход, возвращает API-токен

> Все последующие запросы требуют заголовка:  
> `Authorization: Bearer <ваш_токен>`

---

### 🛍 Скины (каталог)
GET    /api/skins              → список всех скинов
GET    /api/skins/{id}         → информация о скине
POST   /api/skins              → создать скин (только админ)
PUT    /api/skins/{id}         → обновить скин (только админ)
DELETE /api/skins/{id}         → удалить скин (только админ)

---

### 💰 Кошелёк
GET    /api/wallet               → текущий баланс
POST   /api/wallet/top-up        → пополнить кошелёк (например, +100 монет)

---

### 🛒 Покупки
GET    /api/myskins              → список купленных скинов
POST   /api/skins/{id}/buy       → купить скин

> Правила:
> - Нельзя купить один и тот же скин дважды
> - При покупке проверяется баланс
> - Монеты списываются автоматически

---


---

## 🧩 Модели (`app/Models/`)

| Файл | Описание |
|------|--------|
| `User.php`     | Пользователь системы: имя, email, пароль, флаг `is_admin` |
| `Wallet.php`   | Кошелёк пользователя: хранит количество монет (`coins`) |
| `Skin.php`     | Скин: название, тип, цена, изображение |
| `Purchase.php` | Запись о покупке: связь пользователя и скина |

---

## 🔐 Авторизация и политики

| Файл | Описание |
|------|--------|
| `app/Http/Controllers/Api/AuthController.php` | Регистрация и вход, выдача Sanctum-токена |
| `app/Policies/SkinPolicy.php` | Проверка прав: только админ может создавать/редактировать/удалять скины |

---

## 📡 Контроллеры API (`app/Http/Controllers/Api/`)

| Файл | Описание |
|------|--------|
| `SkinController.php`   | CRUD-операции для скинов (с использованием Policy) |
| `WalletController.php` | Получение баланса и пополнение кошелька |
| `PurchaseController.php` | Покупка скина, просмотр купленных скинов |

---

## 🛤 Маршруты

| Файл | Описание |
|------|--------|
| `routes/api.php` | Все API-эндпоинты, сгруппированы по middleware `auth:sanctum` |

---

## 🗄 База данных

### Миграции (`database/migrations/`)
| Файл | Описание |
|------|--------|
| `create_users_table.php`     | Пользователи |
| `create_wallets_table.php`   | Кошельки (связь 1:1 с пользователем) |
| `create_skins_table.php`     | Каталог скинов |
| `create_purchases_table.php` | Покупки (связь many-to-many через промежуточную таблицу) |

